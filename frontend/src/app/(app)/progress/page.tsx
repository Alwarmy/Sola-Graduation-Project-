import { ProgressPageClient } from "@/features/progress/components/ProgressPageClient";

export const metadata = {
  title: "Progress · SOLA",
};

export default function ProgressPage() {
  return <ProgressPageClient />;
}
