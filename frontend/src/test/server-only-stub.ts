// Test stub for the `server-only` package: a no-op so server modules can be
// imported from unit tests. Real server-only enforcement still applies in
// `next build` / runtime via Next.js's bundler boundary.
export {};
